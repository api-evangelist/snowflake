naftiko: "1.0.0-alpha1"

info:
  label: "Snowflake SQL API"
  description: "Submit SQL statements and manage query execution in Snowflake."
  tags:
    - Snowflake
    - SQL
    - Query
  created: "2026-04-18"
  modified: "2026-04-18"

binds:
  - namespace: env
    keys:
      SNOWFLAKE_ACCOUNT_URL: SNOWFLAKE_ACCOUNT_URL
      SNOWFLAKE_JWT_TOKEN: SNOWFLAKE_JWT_TOKEN

capability:
  consumes:
    - type: http
      namespace: snowflake-sql
      baseUri: "{{SNOWFLAKE_ACCOUNT_URL}}"
      description: "Snowflake SQL API"
      authentication:
        type: bearer
        token: "{{SNOWFLAKE_JWT_TOKEN}}"
      resources:
        - name: statements
          path: /api/v2/statements
          description: "SQL statement resources"
          operations:
            - name: submit-statement
              method: POST
              description: "Submit a SQL statement for execution"
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: "$."
              body:
                type: json
                data:
                  statement: "{{tools.statement}}"
                  warehouse: "{{tools.warehouse}}"
            - name: get-statement-status
              method: GET
              description: "Get the status of a submitted statement"
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: "$."
            - name: cancel-statement
              method: POST
              description: "Cancel a running statement"
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: "$."
